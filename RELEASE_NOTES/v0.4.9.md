# MCPAP Vendor Map — Patch v0.4.9

Release date: 2026-04-01

## Mobile map (`/mobile`)

- **Event date on map**: Floating title bar at the top shows the selected day (same long date format as desktop). Shrink-wrapped width, centered, themed border/shadow.
- **Vendor list deep link**: Smoother pan → zoom → highlight pulse when opening from the list; logo/badge sizing is held until programmatic zoom finishes so scales stay matched.
- **Timing**: Shorter gaps before the booth pulse; zoom can start before the pan fully completes for a snappier sequence.
- **Side menu**: “Vendors” section heading removed; link label is **Browse Vendor List** (title-style capitalization).

## Mobile vendor list (`/mobile/vendors`)

- Full-page list with search, shared day filter, and detail sheet; linked from the map menu.
- **Event date** control uses the same card styling as the map drawer (section title + menu-style select).

## Desktop (admin)

- **Profile menu**: **Export vendor data…** and **Import vendor data…** (JSON). Import replaces all booths after confirmation, then saves and reloads.
- **Badge legend** panel: taller max height, vertical scroll on short viewports, thin scrollbar styling.

## Technical

- Asset **cachebuster** bumped to `v0.4.9` for desktop and mobile templates.

---

*Patch release: mobile UX and admin tooling; existing API and data shape unchanged aside from import replacing server-side booth JSON when used.*
